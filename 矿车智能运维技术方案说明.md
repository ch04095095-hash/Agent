# 矿车智能运维技术方案说明

## 一、项目背景

煤矿井下矿车运行环境复杂，具有粉尘大、振动强、网络弱、设备多、风险高等特点。传统运维方式主要依赖人工巡检和事后维修，存在以下问题：

- 故障发现晚，容易造成停机；
- 维修依赖经验，诊断效率不高；
- 现场环境复杂，人工排查成本高；
- 多台车辆分散运行，缺少统一管理手段；
- 关键风险难以及时预警，安全保障压力大。

因此，需要建设一套面向矿车的智能运维系统，形成“**实时监测、风险预警、辅助决策、联动控制、闭环维修**”的一体化能力。

---

## 二、建设目标

本项目的核心目标不是简单地做故障报警，而是实现矿车运维从“事后处理”向“事前预警、事中干预、事后闭环”升级，重点包括：

1. **实时掌握车辆运行状态**，对车速、转速、温度、压力、液位、气体浓度等关键指标进行持续监测；
2. **提前识别故障风险**，在设备真正停机前发现异常趋势；
3. **输出故障原因与维修建议**，提升检修效率；
4. **支持降速、限载、限功率等降级策略**，避免故障扩大；
5. **形成运维闭环**，实现“感知—预测—决策—控制—维修”的完整链路；
6. **适配井下边缘部署**，满足弱网、低时延、强稳定性的工程要求。

---

## 三、系统建设思路

### 3.1 总体思路

借鉴智能驾驶的技术路线，但不直接照搬，而是结合矿车场景特点，构建适合井下环境的智能运维体系。

系统建议采用以下分层思路：

- **感知层**：采集 CAN 数据、传感器数据、环境数据、视觉数据；
- **分析层**：进行规则判断、趋势分析、异常识别、风险评估；
- **决策层**：输出预警等级、故障原因、维修建议、降级策略；
- **控制层**：执行降速、限载、限功率、停车等动作；
- **运维层**：形成记录、报表和维修闭环。

### 3.2 技术路线

建议采用“**规则引擎打底 + 机器学习增强 + 时序预测升级 + 多模态融合演进**”的路线。

- **第一阶段：规则判断**
  - 适合明确阈值类风险，如甲烷超限、温度超限、压力异常等；
  - 优点是稳定、可解释、易落地。

- **第二阶段：传统机器学习**
  - 适合结构化表格数据，如温度、压力、速度、转速、载荷等；
  - 可用于识别高风险状态和故障前兆。

- **第三阶段：时序预测**
  - 适合利用历史趋势判断未来是否会出现异常；
  - 可用于提前预警、寿命预测和趋势分析。

- **第四阶段：多模态融合**
  - 融合 CAN、传感器、图像、环境信息；
  - 支持更复杂的故障识别和现场安全判断。

---

## 四、当前系统基础能力

目前系统已经具备较完整的数据基础，可支撑智能运维的前期建设。

### 4.1 已具备的数据基础

系统可采集和解析的关键数据包括：

- 车速、转速、角度、挡位；
- 甲烷浓度、一氧化碳浓度；
- 冷却水温、表面温度、尾气温度、进气温度；
- 制动压力、行走压力、系统压力、夹紧压力；
- 液压油温、液压油液位、补油压力、灭火系统压力；
- 进气压力、水箱水位、机油压力、柴油液位；
- 报警号、急停状态。

### 4.2 已具备的基础能力

这些数据已经能够支撑：

- 基础状态监测；
- 简单阈值告警；
- 运行趋势记录；
- 故障解释和维修建议输出；
- 后续机器学习和时序预测模型接入。

换句话说，系统已经不再是“单纯采集数据”，而是具备了升级为智能运维平台的基础。

---

## 五、当前重点推进的方向

### 5.1 重点方向一：故障前预警

相比“故障后报警”，更重要的是在故障发生前识别风险。例如：

- 温度持续上升但尚未超限；
- 压力波动异常但尚未触发停机；
- 车速、转速和负载之间出现不合理关系；
- 视觉识别到障碍物或环境风险。

这类能力可以帮助系统从“报警系统”升级为“预防性运维系统”。

### 5.2 重点方向二：故障原因解释

领导和现场人员最关心的问题通常不是“是否异常”，而是：

- 为什么异常；
- 哪个部件有问题；
- 是否需要停机；
- 如何处理最合适。

因此，系统需要输出更清晰的故障解释和维修建议，而不是只给一个简单告警。

### 5.3 重点方向三：降级运行策略

并不是所有异常都必须立即停机。对于部分非致命异常，可采取：

- 降速；
- 限载；
- 限功率；
- 进入保守模式；
- 提前安排检修。

这种方式既能保障安全，也能减少不必要的停机损失。

### 5.4 重点方向四：边缘端智能决策

由于井下网络不稳定，系统必须能够在边缘端独立运行，不能完全依赖云端。也就是说，关键判断必须在本地完成，云端主要做汇总、分析和优化。

---

## 六、总体架构建议

建议将系统分为五层：

### 6.1 数据采集层

采集来源包括：

- CAN 总线；
- 车载传感器；
- 环境传感器；
- 视觉模块；
- 运行和维修记录。

### 6.2 数据治理层

负责对原始数据进行：

- 时间对齐；
- 清洗去噪；
- 缺失处理；
- 标准化归一；
- 标签整理。

### 6.3 健康评估层

输出当前车辆及各子系统的健康状态，包括：

- 健康分；
- 风险等级；
- 异常类型；
- 趋势变化情况。

### 6.4 风险预测层

基于历史数据和实时变化，预测：

- 未来一段时间内是否会出现异常；
- 异常可能属于哪一类；
- 风险会不会继续扩大。

### 6.5 决策控制层

根据风险等级执行：

- 预警提示；
- 降级运行；
- 停机保护；
- 维修建议；
- 运维工单生成。

---

## 七、现有架构的优势

当前架构已经具备较好的工程基础，主要优势体现在以下几个方面：

### 7.1 数据链路清晰

系统从 CAN 解码到状态输出，再到在线处理和决策判断，已经形成了比较完整的流程。

### 7.2 具备规则兜底

在模型之外，系统可以通过规则先行保障安全，避免完全依赖算法带来的不确定性。

### 7.3 具备在线窗口处理能力

系统支持对短时间窗口内的数据进行聚合，这对于趋势识别和风险预警非常关键。

### 7.4 适合边缘部署

系统设计方向符合井下场景对本地实时性和稳定性的要求。

---

## 八、后续改造建议

如果要进一步提升系统能力，建议重点推进以下工作：

### 8.1 增强时序特征能力

增加：

- 最近 N 秒均值；
- 变化率；
- 波动幅度；
- 趋势斜率；
- 异常跳变检测。

### 8.2 增强风险预测能力

引入轻量机器学习模型，用于预测：

- 短时风险；
- 故障前兆；
- 异常升级趋势。

### 8.3 增强决策解释能力

系统应输出更加明确的：

- 异常原因；
- 可能故障部位；
- 建议动作；
- 是否建议停机。

### 8.4 增强边缘端鲁棒性

重点考虑：

- 断网续传；
- 本地缓存；
- 低带宽传输；
- 关键决策本地化。

---

## 九、边缘端 LLM 使用原则

对于大模型，只把它作为辅助解释和辅助生成工具，不要让它承担实时控制职责。

### 9.1 适合做的事情

- 故障原因解释；
- 维修建议生成；
- 报告摘要；
- 人机交互问答；
- 运维知识检索。

### 9.2 不适合做的事情

- 实时安全控制；
- 直接下发停车指令；
- 高频重复推理；
- 过长上下文分析。

### 9.3 推荐原则

- 控制逻辑由规则和程序负责；
- 风险预测由轻量模型负责；
- 大模型负责解释和辅助决策。

这样既能保证实时性，也能避免算力浪费。

---

## 十、结论

总体来看，矿车智能运维系统已经具备向“预防性、智能化、边缘化、闭环化”升级的基础条件。

下一阶段的重点不应只是“多采集数据”，而应聚焦于以下四件事：

1. **提前发现风险**；
2. **准确解释故障原因**；
3. **支持降级运行和联动控制**；
4. **在边缘端形成稳定可靠的决策能力**。

最终目标是把系统建设成一个真正能够服务矿车安全运行和运维效率提升的智能平台。
